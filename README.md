This is a plugin for the server software endstone only on linux

INSTALL
download the file as a zip or the folder, set up a venv the run

pip install .

or

pip install . --force-reinstall --no-deps

